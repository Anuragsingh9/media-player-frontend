import React from "react";

const VideoTitle = (props) => {

    return (
        <>
            <div>
                <span>
                    {props.title}
                </span>
            </div>
        </>
    )
}

export default VideoTitle;